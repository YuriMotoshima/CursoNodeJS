{% extends "layout.njk" %}

{% block content %}
    <main>

        <div class="content">

            {% for item in items %}

                <div class="card">
                    <img src="{{item.image}}" alt="Imagem de {{item.food}}">
                    <h4>{{item.food}}</h4>
                    <p>por
                        {{item.chef}}</p>
                </div>

            {% endfor %}

            {% for item in items %}

                <div class="card">
                    <img src="{{item.image}}" alt="Imagem de {{item.food}}">
                    <h4>{{item.food}}</h4>
                    <p>por
                        {{item.chef}}</p>
                </div>

            {% endfor %}

            {% for item in items %}

                <div class="card">
                    <img src="{{item.image}}" alt="Imagem de {{item.food}}">
                    <h4>{{item.food}}</h4>
                    <p>por
                        {{item.chef}}</p>
                </div>

            {% endfor %}

        </div>

    </main>
{% endblock content %}

{% block modal %}
    <div class="modal-overlay">
        <div class="modal">
            <div class="modal-content">
                <div class="card"></div>
                <img src="" alt="">
                <h4></h4>
                <p></p>
                <a class="close-modal" href="#">Fechar Modal</a>
            </div>
        </div>
    </div>

    <script src="/scripts.js"></script>

{% endblock modal %}
</body>

</html>