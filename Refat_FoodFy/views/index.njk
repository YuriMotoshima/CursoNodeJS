{% extends "layout.njk" %}

{% block content %}

    <section class="hero">
        <div class="intro">
            <h1>As melhores receitas</h1>
            <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
        </div>
        <img src="/chef.png" alt="">
    </section>

    <main>

        <h2>Mais acessadas</h2>

        <div class="content">

            {% for item in items %}

                <div class="card">
                    <img src="{{item.image}}" alt="Imagem de {{item.food}}">
                    <h4>{{item.food}}</h4>
                    <p>por
                        {{item.chef}}</p>
                </div>

            {% endfor %}
        </div>

    </main>

{% endblock content %}